# 版本历史 / Changelog

> 这里记录每一次发布改了什么。**日常使用只需要到 [Releases](../../releases) 下载最新版**；
> 本文件存在的意义是：旧版为什么消失、各版差在哪、要旧版去哪儿拿，都有据可查。

---

## v1.2 — 2026-09-24 ✅ 当前版本

**附件**：`HotlineMiami2_CN_Pack_v1.2.zip`（87,920,854 B）
**md5** `c6ac330888a36949d0ccfa0c62fce6a7` ｜ **sha256** `a79997c4955abe9f7ad896e1d78fac99ddc54417231fb3898ce5adbb35e4dfb1`

**主题：把关卡编辑器"里面"全部补完，包括最难啃的「物品页」—— 真·全汉化。**

v1.1 的 56 条只覆盖了编辑器的**外壳**（浏览器/管理界面）。真正进编辑器画面后，
页签、操作提示、角色/敌人选择弹窗、`FILE/EDIT/HELP` 菜单、Perks、设置项**依旧是英文**；
而**「物品页」整个列表也是英文**。玩家接连截图反馈，本版把这三块一次性补齐。

### ① 编辑器画布内部：新增 345 条

编辑器页签 `BUILD / ITEMS / ENEMY / MISC / LEVEL` → **建造 / 物品 / 敌人 / 杂项 / 关卡**，
地板分页、画布操作提示、角色与敌人选择弹窗、关卡信息栏（`AUTHOR`/`S-RANK SCORE`/日期栏）、
封面与楼层切换、菜单栏全部子项、过场编辑器、Perks 强化项、设置项、警告提示、战役编辑器……

### ② 敌人页与弹窗：新增 84 条

- 敌人行为名 `STATIC/PATROL/RANDOM/FAT/DOG/DODGER/IDLE` → **静止/巡逻/随机/胖/狗/闪避/待机**及 5 条行为说明；
- 编辑器弹窗与警告全批汉化：`未保存的更改将丢失！`、`确定要清空全部瓷砖/墙壁/物件/敌人？`、
  `HOTLINE EDITOR` 关于框、`REMOVE/ADD RAIN·DARKNESS·DAYLIGHT` 等；
- 过场编辑器 22 条动作描述片段（`walk to / change sprite to / Wait / Rotate` …）。

### ③ 物品页汉化：显示名 827 条（本版重点）

**这是编辑器里唯一不能靠"翻译表"解决的地方，而且改错还会静默丢条目。** 物品页的列表
**不是界面文字**——它的数据源是 `GL\hlm2_objects.bin` 里的 **obj 名字池**，**显示名 = 去掉开头的 `obj`**；
编辑器再拿这个名字后缀去查贴图 `spr<后缀>` / `sprElis<后缀>` / `sprELis<后缀>`，
**查不到就把整条静默丢掉**（不报错、不写日志）。所以改 `hlm2_localization.bin` 永远是英文，
**只能把 obj 名与贴图名一起改成中文**。

硬约束：**新名字节数必须与旧名完全相等**，否则 WAD 目录树 / 贴图池偏移 / 关卡文件长度前缀全要重建。
旧名能借的余量又很小（`Desk` 4 字节、`ATM` 3 字节）—— **「中文 + 完整英文」数学上不可能**（0/796 放得下）。

做法：把 821 个词素建成 **1160 条中文词典**，做**最长优先匹配 + 词边界校验**
（避免 `Jacket` 被切成人名、`AirCondition` 被拆成"空气+空调"），
再「能缩字就缩字 → 缩不动才从开头丢词」压到预算内，最后**把 obj 池与贴图池里同一后缀的所有变体
（`obj` / `spr` / `sprElis` / `sprELis` / `msk`…）同步替换**，用空格补齐到**与原名等长**。
只改贴图不改 obj，配对就会断（实测从 1364 条掉到 343 条）——"物品变少"就是这么来的。

- **等长改名 = 零结构风险**：改完 WAD 体积 **141,519,203 B 一字节不差**，4522 个条目偏移全不受影响；
- 全量铺开结果：**显示名 827 条**（obj 池）+ **贴图资源名 1014 条**（spr/msk 池），
  合计替换 **9,962 处**引用，**旧名残留 0、重名冲突 0、缺字形 0**；官方 78 个关卡已同步改名；
- **配对一致性：827 条中文条目全部能查到贴图，断 0 条**；
- 另有 **43 条人名/乐队名与 3 字节短名**（`Jacket`、`Nicke`、`ATM`、`Box`…）按惯例保留英文；
- 短名只能写 1～2 个字（`BankDesk → !银书`、`VIPArmchair → !贵宾扶`），属预算所迫的取舍。

> **关于显示名前面的 `!`**：列表按 ASCII 排序，且引擎只索引 A–Z —— 中文（UTF-8 以 0xE5 起）
> 会排到全部英文之后**且进不了字母索引**（表现为"好像没汉化"）。加 `!`(0x21) 中文才能排到最前，
> 这是引擎限制下的功能性处理，不是笔误。

> ⚠️ **副作用**：物品页改的是资源名本身，**玩家自制关卡 / 创意工坊关卡引用的是旧名字**，
> 装上本版后可能出现物件、贴图丢失。官方 78 个关卡不受影响；要玩自制关卡先还原英文。

### 其他

- exe 硬编码文本 **121 → 550 条**（成就 65 + 编辑器/界面 485）；
- 汉字字形 **1343 → 1600**，76 套字体逐字体比对 **0 缺字**；
- 仍保持 v1.2 内部迭代阶段修掉的两个隐患（见下）：字节预算严格只借用填充 0；安装器不带全文模糊搜索、完全幂等；
- 分发版 `patch_exe.ps1` 在纯净原版 exe 上重打一遍，与作者实机版本 **md5 完全一致**。

刻意保留英文的部分：3 个成就名 `1-800-*`（显示名即 Steam API 标识符）、编辑器武器/物件名（资源标识符）、
全大写内部标识符、按键名、人名/歌名，以及 255 条落在 exe 拼接片段上的资源名（`Biker*`/`Cobra*`/`Colombian*` 等）。

---

## v1.1 正式版 — 2026-09-24

**附件**：`HotlineMiami2_CN_Pack_v1.1.zip`（83,077,775 B）
**md5** `d3815ce775a989924d33322292b8f83f` ｜ **sha256** `efcbd74da9e34bdab98948feb9da301135c0ca0c8e15033105219fc508805423`

解决 v1.1 初版遗留的两个问题：

- **成就页终于看得清**：中文字形从「黑体常规体」换成**微软雅黑粗体**，字号取该字体行高允许的最大值，
  并重算字距 —— 原来 10 像素下横画整根丢失、字迹发虚；
- **关卡编辑器 UI 全汉化（56 条，仅外壳）**：编辑器文案和成就一样硬编码在 `HotlineMiami2.exe` 里
  （`hlm2_localization.bin` 里一条都没有），本版一并处理；
- 补齐 `坊 / 建 / 役 / 签` 4 个字形，共 **1343 个汉字字形**，76 套字体 0 缺字；
- 安装器按 `exe_map.json` 的**文件偏移**替换 **121 条**硬编码文本（23 成就名 + 42 成就描述 + 56 编辑器 UI），
  全部命中、结果可复现（与实际验证过的 exe md5 一致），重复执行安全。

---

## v1.1 初版（已撤回，不再提供下载）— 2026-09-23

**附件**：`HotlineMiami2_CN_Pack_v1.1.zip`（83,810,684 B）— **已删除**

- 首次加入**成就名 / 成就描述汉化**：这两块硬编码在 `HotlineMiami2.exe` 里，只改 WAD 无效，
  本版对 exe 做等长字节替换（Steam 成就 ID 不变）；
- **问题**：中文字形用「黑体常规体」，在 10px 下笔画断裂、发虚，实际观感"看不清"；
- 该版本的包体在仓库整改时连同 release 一起**被删除，GitHub 不可恢复**，本地也未留存。
  如果你手上的旧包写着 v1.1 且成就字迹发虚，那就是这一版，请换用当前版本。

> **编号说明**：这一版曾短暂公开发布，随后被撤下。为保持版本号连续（v1.0 → v1.1），
> 正式版沿用 **v1.1** 编号。因此**网上出现过的 v1.1 有两个**：
> 2026-09-23 的初版（成就发虚）与 2026-09-24 的正式版。以附件大小区分：
> 初版 83,810,684 B，正式版 **83,077,775 B**。

---

## v1.0（历史版本，仅文本汉化）— 2026-09-20

**附件**：`HotlineMiami2-CN-Patch-v1.0.zip`（26,037,897 B）
**md5** `2349c3e232b20f162a94fb377148d9c8`

- **只汉化文本**：菜单 / 操作提示 / HUD / 对话 / 字幕 / 章节标题，共 2889 条；
- 采用**轻量注入版**：包里只有补丁数据（`patchdata/` 内 1 个中文文本库 + 152 个字体文件），
  安装时由 `install.ps1` 注入到**你自己电脑上的**游戏 wad 里，安装前自动备份；
- **不改动 exe**：所以成就页、关卡编辑器**仍是英文**（这正是 v1.1 要补的两块）；
- 字形数 1340（黑体常规体）。

> 本版曾于 2026-09-20 公开发布，后在仓库整改时被删除。**由于原包在本地留有副本，
> 现已重新挂到 Releases 作为历史存档**（标注为历史版本，功能上被 v1.1 完全取代）。

---

## 未发布的内部迭代（已并入 v1.2，不单独发版）

v1.1 到 v1.2 之间做过两轮内部构建，**从未在 GitHub 上发布过**，其内容已全部并入上面的 v1.2：

| 内部代号 | 内容 | 去向 |
|---|---|---|
| v1.2-编辑器内部汉化（84,185,197 B） | 编辑器画布内部面板与菜单 345 条 | 已并入 v1.2 第 ① 块 |
| v1.3-敌人页与弹窗（84,315,315 B） | 敌人行为名/说明、弹窗警告、过场动作 84 条 | 已并入 v1.2 第 ② 块 |

这两轮还顺带修掉两个 v1.1 就存在的隐患，v1.2 一并继承：

1. **字节预算算法收紧**——原算法把「到下一个文本串的距离」当可写长度，
   但部分位置串后紧跟的是**浮点常量等二进制数据**（如偏移 20069382 的 `0xD5`），会误伤。
   现改为**只借用紧跟其后的填充 0 并保留终止符**。实测 550 条的改动字节 **100% 落在槽位内**。
2. **安装器移除"全文模糊搜索"兜底**——旧兜底会在**重复安装**时用短英文串（如 `OK`）
   匹配到别的单词内部，静默改坏 8 处。现改为严格按偏移比对英文 + 幂等识别。
   连跑 3 次：第 1 次换 550 条，第 2/3 次 0 替换、550 条已是最新，**哈希不再变化**。

---

## 版本一览

| 版本 | 日期 | 文本 | 成就页 | 关卡编辑器 | 字体 | 包体 | 状态 |
|---|---|---|---|---|---|---|---|
| v1.0 | 09-20 | ✅ 2889 条 | ❌ 英文 | ❌ 英文 | 黑体常规体 1340 字 | 26,037,897 B | 历史存档 |
| v1.1 初版 | 09-23 | ✅ | ✅ | ❌ 英文 | 黑体常规体 | 83,810,684 B | 已撤回（包体不可恢复） |
| v1.1 正式版 | 09-24 | ✅ | ✅ 清晰 | ⚠️ 仅外壳 56 条 | 微软雅黑粗体 1343 字 | 83,077,775 B | 已被 v1.2 取代 |
| **v1.2** | **09-24** | ✅ | ✅ | ✅ **内部+敌人页+物品页 485 条 + 物品页显示名 827 条** | **微软雅黑粗体 1600 字** | **87,920,854 B** | **当前版本** |

## 关于"旧版去哪了"

2026-09-24 的仓库整改（把公开仓库下架）中，v1.0 与 v1.1 初版的 release、附件、tag 被一并删除。
**GitHub 没有回收站，已删除的 release 附件无法恢复**：

- v1.0 的包在本地留有副本 → 已重新挂回 Releases；
- v1.1 初版的包本地未留存 → 无法恢复（那也正是"成就看不清"的版本，不建议使用）。

今后每次发版都会**先**把安装包归档到仓库外的本地目录，**再**上传 GitHub，避免再次出现"旧版找不回"。
